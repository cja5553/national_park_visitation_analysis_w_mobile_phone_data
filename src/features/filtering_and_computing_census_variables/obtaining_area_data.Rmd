```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

# Install the neccessary Packages

```{r}
require(sp)  
require(rgdal)
require(maps)
args = commandArgs(trailingOnly=TRUE)
```




```{r}
library(raster)
poly <- readOGR("national_park_visitation_analysis_w_mobile_phone_data/volume/data/raw/national_parks","National_Parks")
crs(poly)
poly$area_sqkm <- area(poly) / 1000000

area_data<-data.frame(poly$PARKNAME, poly$area_sqkm)
write.csv(area_data, "national_park_visitation_analysis_w_mobile_phone_data/volume/data/interim/census_files_cleaned_and_computed/area_data.csv
")
```

